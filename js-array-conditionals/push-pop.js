var number = [45, 55, 65, 75];
// if we want to add further numbers into this array then we can do like this.......
// step 1: we have to write down the array name first,
// step 2: after that we have to type .push. .push means to add something in the existing array,
// step 3: then add the new variables like this .push(85),

number.push(85);
console.log(number);

// another example including strings.........
var salad = ['carrots', 'tomatoes', 'cabbage'];
salad.push('cucumbers');
console.log(salad);